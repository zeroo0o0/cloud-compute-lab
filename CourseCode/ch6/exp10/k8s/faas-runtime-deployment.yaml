apiVersion: apps/v1
kind: Deployment
metadata:
  name: faas-runtime
  labels:
    app: faas-runtime
spec:
  replicas: 1
  selector:
    matchLabels:
      app: faas-runtime
  template:
    metadata:
      labels:
        app: faas-runtime
    spec:
      containers:
      - name: runtime
        image: ch6-exp10-runtime:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 9000
        env:
        - name: PORT
          value: "9000"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
