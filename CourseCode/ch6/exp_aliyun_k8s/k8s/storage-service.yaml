apiVersion: v1
kind: Service
metadata:
  name: storage
  labels:
    app: storage
spec:
  selector:
    app: storage
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
  type: ClusterIP
